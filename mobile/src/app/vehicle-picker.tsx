import { View, Text } from 'react-native'

export default function VehiclePicker() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Vehicle picker — next task</Text>
    </View>
  )
}
