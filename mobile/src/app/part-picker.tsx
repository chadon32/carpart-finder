import { View, Text } from 'react-native'

export default function PartPicker() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Part picker — next task</Text>
    </View>
  )
}
